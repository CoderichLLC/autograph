{
  "name": "autograph",
  "workspaces": [
    "workspace/*"
  ],
  "scripts": {
    "lint": "eslint ./",
    "test": "npm run test -ws",
    "clinic-test": "npm run clinic-test -ws"
  },
  "devDependencies": {
    "@coderich/dev": "0.6.3",
    "clinic": "13.0.0"
  }
}
